import 'package:flutter/material.dart';

class FinalProductStoreDesktopLayout extends StatelessWidget {
  const FinalProductStoreDesktopLayout({super.key, required this.pageName});
  final String pageName;

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Final Product Store desktop Layout"));
  }
}
